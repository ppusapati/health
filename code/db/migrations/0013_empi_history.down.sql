-- Reverses 0013.
--
-- Prior names stop being searchable, which re-opens the path where a result
-- addressed to a maiden name finds nobody and becomes a second patient record.
-- Caregiver authorities vanish rather than expiring. For a failed deploy
-- against an empty database only.

DROP TABLE IF EXISTS empi.related_person;
DROP TABLE IF EXISTS empi.communication_preference;
DROP TABLE IF EXISTS empi.patient_name;
