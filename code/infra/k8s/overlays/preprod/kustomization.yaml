apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: healthcare-preprod

resources:
  - ../../base

replicas:
  - name: core
    count: 2

patches:
  - target:
      kind: ConfigMap
      name: core-config
    path: config-patch.yaml
